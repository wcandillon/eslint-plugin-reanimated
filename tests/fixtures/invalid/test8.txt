function fn() {
  return true;
}

function foo() {
  "worklet";
  return true;
}

const test = (
) => {
  "worklet";
  const test = () => {
    const bar = () => true;
    if (Math.random()) {
      foo();
      bar();
    }
  }
  if (Math.random()) {
    fn();
  }
};

const bar = () => {
  const {x} = { x: 1 };
  clamp(1, ...[1, 2]);
}